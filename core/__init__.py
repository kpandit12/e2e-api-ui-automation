"""Cross-cutting collaborators for the API framework.

This package holds the reusable, independently-testable objects that are
*composed* into the client layer: the exception hierarchy, structured logging,
retry strategies, the request logger, and the auth-token provider.
"""
