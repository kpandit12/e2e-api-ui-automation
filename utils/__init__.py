"""Layer 3 — orchestration utilities.

Plain modules (not stateless classes) that compose several DAO calls into a
single reusable business operation, so multi-step logic lives above the tests
instead of being copy-pasted inside them.
"""
